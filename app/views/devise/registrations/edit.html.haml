= render "shared/top_page/header"
- breadcrumb :email_password
= breadcrumbs separator: " &rsaquo; "

.main__container.clearfix
  = render partial: 'shared/mypage/side_menu'

  .main__content
    %section.chapter__container
      %h2.chapter__head メール/パスワード 
      = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'chapter__container__inner', method: :put }) do |f|
        .form__main__container
          .form__content__default  
            = f.label :email, 'メールアドレス'
            = f.email_field :email,placeholder: @user.email, class: "email" 
            = render partial: "devise/shared/error_messages", locals: { resource: resource, attr: :email }
            .form__content__default--text
              メールアドレスを変更すると確認メールが送信されます。メール内のURLをクリックすると変更完了です。
          .form__content__default
            = f.label :current_password, '現在のパスワード' 
            = f.password_field :current_password,placeholder: "6文字以上", class: "email" 
            = render partial: "devise/shared/error_messages", locals: { resource: resource, attr: :current_password }
          .form__content__default 
            = f.label :password, '新しいパスワード' 
            = f.password_field :password,placeholder: "6文字以上", class: "email" 
            = render partial: "devise/shared/error_messages", locals: { resource: resource, attr: :password }
          .form__content__default 
            = f.label :password_confirmation, '新しいパスワード確認' 
            = f.password_field :password_confirmation,placeholder: "6文字以上", class: "email" 
            = render partial: "devise/shared/error_messages", locals: { resource: resource, attr: :password_confirmation }
            .form__content__default--text
              パスワードを設定したい場合は上記をすべて入力してください。（設定しない場合は空欄）
          = f.submit '変更する', class: 'red__btn'
          
= render "shared/top_page/sell-fix-logo"
= render "shared/top_page/app-banner"
= render "shared/top_page/footer"
